[package]
name = "lsystem-core"
version = "0.1.0"
edition = "2024"
license.workspace = true

[features]
svg = []

[dependencies]
glam = { workspace = true, features = ["bytemuck"] }
thiserror.workspace = true
