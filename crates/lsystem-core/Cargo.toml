[package]
name = "lsystem-core"
version = "0.1.0"
edition = "2024"
license.workspace = true

[features]
svg = []

[dependencies]
glam = { workspace = true, features = ["bytemuck"] }
thiserror.workspace = true

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "generation"
harness = false
